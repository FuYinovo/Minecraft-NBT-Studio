<?xml version="1.0" encoding="utf-8"?>

<mapEditorButton:ButtonBase
    x:Class="NBT_Studio.Xaml.Control.MapEditorButton.ToggleButtonCompact"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:inter="using:Microsoft.Xaml.Interactivity"
    xmlns:behavior="using:NBT_Studio.Xaml.Behavior"
    xmlns:mapEditorButton="using:NBT_Studio.Xaml.Control.MapEditorButton"
    mc:Ignorable="d">


    <Grid>
        <ToggleButton
            Height="26"
            Width="32"
            Padding="0"
            Background="Transparent">
            <FontIcon Glyph="{x:Bind FontIcon}" FontSize="16" />
            <inter:Interaction.Behaviors>
                <behavior:MutuallyToggleButtonsBehavior GroupName="{x:Bind GroupName}" Tag="{x:Bind MemberTag}" />
            </inter:Interaction.Behaviors>
        </ToggleButton>
    </Grid>
</mapEditorButton:ButtonBase>