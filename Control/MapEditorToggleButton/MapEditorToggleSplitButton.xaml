<?xml version="1.0" encoding="utf-8"?>

<mapEditorToggleButton:MapEditorToggleButtonBase
    x:Class="NBT_Studio.Control.MapEditorToggleButton.MapEditorToggleSplitButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:inter="using:Microsoft.Xaml.Interactivity"
    xmlns:behavior="using:NBT_Studio.Xaml.Behavior"
    xmlns:mapEditorToggleButton="using:NBT_Studio.Control.MapEditorToggleButton"
    mc:Ignorable="d">

    <mapEditorToggleButton:MapEditorToggleButtonBase.Resources>
        <Style x:Key="ToolsLabel" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </mapEditorToggleButton:MapEditorToggleButtonBase.Resources>

    <Grid>
        <StackPanel Spacing="4">
            <ToggleSplitButton Background="Transparent"
                               BorderBrush="{ThemeResource AppBarToggleButtonPointerOverBackgroundThemeBrush}"
                               Height="60"
                               Margin="0,-6,0,-6">
                <FontIcon Glyph="{x:Bind FontIcon}" FontSize="18"></FontIcon>
                <inter:Interaction.Behaviors>
                    <behavior:MutuallyToggleSplitButtonsBehavior
                        GroupName="{x:Bind GroupName}"
                        Value="{x:Bind MemberName}">
                    </behavior:MutuallyToggleSplitButtonsBehavior>
                </inter:Interaction.Behaviors>
                <ToggleSplitButton.Flyout>
                    <MenuFlyout x:Name="MenuFlyout" />
                </ToggleSplitButton.Flyout>
            </ToggleSplitButton>
            <TextBlock Style="{StaticResource ToolsLabel}" Text="{x:Bind Title}"></TextBlock>
        </StackPanel>
    </Grid>
</mapEditorToggleButton:MapEditorToggleButtonBase>